export const ROUTES = {
  index: '/',
  api: '/api',
  assets: '/assets',
  image: '/assets/:imgSlug',
  login: '/login',
  logout: '/logout',
  register: '/register',
  dashboard: '/dashboard',
} as const
